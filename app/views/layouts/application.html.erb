<!DOCTYPE html>
<html>
  <head>
    <title>Mario's Specialty Foods</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div id="container-test" class="container">
      <div class="row">
        <div class="col-md-2"></div>
        <div class="col-md-8">
          <nav class="navbar navbar-light bg-light">
            <a class="navbar-brand" href="/">Home</a>
            <a class="navbar-brand" href="/products">Products</a>
          </nav>
          <% if current_user %>
            <%= current_user.email %> | <%= link_to "Sign out", '/signout' %>
          <% else %>
            <%= link_to "Sign up", '/signup' %> | <%= link_to "Sign in", '/signin' %>
          <% end %>

    <%= flash[:alert] %>
    <%= flash[:notice] %>
    <%= id="vegetable-pic" image_tag('vegetable.png') %>
          <%= yield %>
        </div>
        <div class="col-md-2"></div>
      </div>
    </div>
  </body>
</html>

