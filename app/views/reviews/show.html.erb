<div class="jumbotron">
  <h1><%= @product.name %>
  <h3> Review by: <%= @review.author %></h3>
  <h3> Rating: <%= @review.rating %></h3>
  <p><%= @review.content_body %></p>
  </div>
<button><%= link_to "back to product", product_path(@product) %></button>
